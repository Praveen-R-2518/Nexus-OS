# n8n workflow exports

Importable snapshots of the Nexus OS n8n workflows (instance `knurdz3o.app.n8n.cloud`).

| File | Workflow | ID | Source |
|---|---|---|---|
| `wf0a_gmail_intake.json` | WF0a Gmail Intake | `bhGCrTSHrj91ojby` | generated by `scripts/build_n8n_workflow_exports.js` |
| `approval_trigger.json` | Approval Trigger (Channel Sender) | — | hand-authored (task 1.3), not yet created live |
| `wf2_classification.json` | WF2 AI Classification | `MmA7EKsOYAZgx3ep` | **re-wired + pushed live 2026-07-17 (second pass)** — `Classify Message` now POSTs to app `/api/internal/n8n/ai/classify` (no direct `api.openai.com`), `Create Lead` POSTs to app `/api/internal/n8n/leads` (no Supabase service-role) — **active** |
| `wf1_message_intake.json` | WF1 Message Intake | `zU8cDHJeoUGWbUgC` | **exported from live** (2026-07-12) — **active** |
| `wf3_revenue_rescue.json` | WF3 Revenue Rescue Agent | `OjFlX2W2xYbl5roY` | **re-wired + activated live 2026-07-17 (second pass)** — `Generate Reply Draft` now POSTs to app `/api/internal/n8n/ai/draft` instead of using an n8n-native OpenAI Credential (which was never bound, blocking the earlier activation attempt) — **active** |
| `wf4_followup_scheduler.json` | WF4 Follow-up Scheduler | `qWHvc2AmqX10jEjk` | **re-wired + activated live 2026-07-17 (second pass)** — `Generate Follow-up Message` now POSTs to app `/api/internal/n8n/ai/draft`, same fix as WF3 — **active** |
| `wf0d_ledger_drain.json` | WF0d Ledger Drain | `lr4HzWo2QeghXxhH` | **hardened 2026-07-17** — schedule → app `/api/internal/n8n/inbound-replay` (no Supabase service-role) — **active**, 3 nodes |
| `wf0e_gmail_backfill.json` | WF0e Gmail Backfill | `Y54F1bZLJkRyexTH` | **hardened 2026-07-17** — schedule → app `/api/internal/n8n/gmail-backfill` (no Supabase fetch) — **active**, 3 nodes |
| `wf0f_gmail_sync.json` | WF0f Gmail Sync | `rNjW8GyWfZHuXnnf` | **exported from live** (2026-07-17) — **active**, 2 nodes |
| `wf_error_alerts.json` | WF Error Alerts (stub) | `wfErrorAlertsStub001` | **stub 2026-07-17** — Error Trigger → future app workflow-logs endpoint — **inactive** |
| `wf5_daily_buy_back_report.json` | WF5 Daily Buy-Back Report | `QoJIseLTX2jwDYEy` | **exported from live** (2026-07-17) — **active**, 21 nodes |
| `wf8b_social_publish.json` | WF8b Social Post Publishing | `VZ9ZaA1S2JxSAeGQ` | **exported from live** (2026-07-17) — **active**, 9 nodes — webhook `publish-social-post` → fetch tokens → per-platform publish → mark published |
| `wf8c_post_image_generation.json` | WF8c AI Post Image Generation | `RfmuS0guiaq64Lrx` | **deactivated live 2026-07-17 (second pass)** — superseded by the 2026-07-15 in-app OpenAI posts rebuild (`lib/posts/ai.ts`), and its direct `api.openai.com` calls target a key being retired for Azure — **inactive**, 10 nodes, webhook `generate-post-image` |
| `wf8d_social_scheduler.json` | WF8d Social Post Scheduler | `47BO0agxAJGizttR` | **`$env` → `$vars` fixed in export + pushed to live draft 2026-07-17 (second pass)**, still **inactive** (correct — gated on WF8b's platform credentials), 4 nodes — Schedule Trigger → claim due posts → trigger WF8b |

## Social posting: publish + schedule contract (2026-07-15)

The app now drives publishing directly (no approval queue). Two n8n workflows complete the loop:

**WF8b — Social Post Publishing** (webhook `POST /webhook/publish-social-post`, id `VZ9ZaA1S2JxSAeGQ`)
- Already implemented live (9 nodes). Trigger payload from the app
  (`app/api/posts/publish/route.ts`): `{ orgId, postId, platforms[] }`. The app flips the post to
  `publishing` before calling; WF8b reads the row from Supabase by `postId`.
- Steps: `GET {NEXUS_APP_URL}/api/internal/n8n/social-credentials?organization_id={orgId}`
  (Bearer token) → build per-platform items → `Post to Platform` (IG/FB Graph API, X API v2) →
  `Mark Post Published` (PATCH `social_posts.status='published'`).
- Set the app env `N8N_SOCIAL_PUBLISH_WEBHOOK_URL` to
  `https://knurdz3o.app.n8n.cloud/webhook/publish-social-post`.
- Remaining to make it fully live: bind each platform's publishing credential (Meta/X/LinkedIn app
  review pending). Optional hardening: on failure, POST to `/api/internal/n8n/post-result` with
  `{ postId, status: 'failed', error }` so the post leaves `publishing` instead of hanging.

**WF8d — Social Post Scheduler** (Schedule Trigger every 5 min, id `47BO0agxAJGizttR`)
- `GET {NEXUS_APP_URL}/api/internal/n8n/scheduled-posts` (Bearer `N8N_INGEST_TOKEN`) — this endpoint
  **atomically claims** due posts (`status=scheduled AND scheduled_at<=now` → flipped to `publishing`)
  and returns them, so overlapping polls never double-publish.
- `Expand Posts` (one item per post) → `Trigger Publish` (POST
  `{{ $vars.N8N_SOCIAL_PUBLISH_WEBHOOK_URL }}` — n8n Variable, not `$env`, per the Cloud
  restriction noted below — with `{ orgId, postId, platforms }`) → WF8b publishes each.
- **Activate it** in the n8n UI once WF8b's platform credentials are bound.

**Internal endpoints added (app):** `GET /api/internal/n8n/scheduled-posts` (claim due posts) and
`POST /api/internal/n8n/post-result` (terminal `published`/`failed` writeback — the browser can't
write these). Both Bearer `N8N_INGEST_TOKEN`.

App env required: `N8N_SOCIAL_PUBLISH_WEBHOOK_URL` (app → WF8b). n8n env required:
`NEXUS_APP_URL` + `N8N_INGEST_TOKEN` + `N8N_SOCIAL_PUBLISH_WEBHOOK_URL`, plus each platform's
publishing credential on WF8b.

## n8n auth hardening (2026-07-17)

Scheduler/claim workflows (`wf0d`, `wf0e`, `wf0f`) and WF2 lead creation **must call the Next.js app** with Bearer `N8N_BOOTSTRAP_TOKEN` (or legacy `N8N_INGEST_TOKEN`). **Remove** the Supabase service-role credential from live n8n after re-importing these exports.

| Workflow | App endpoint | Token |
|---|---|---|
| WF0d Ledger Drain | `POST /api/internal/n8n/inbound-replay` | bootstrap / ingest |
| WF0e Gmail Backfill | `POST /api/internal/n8n/gmail-backfill` | bootstrap / ingest |
| WF0f Gmail Sync | `POST /api/internal/n8n/gmail-sync` | bootstrap / ingest |
| WF2 Create Lead | `POST /api/internal/n8n/leads` | bootstrap / ingest |

**Re-import required:** after pulling these repo changes, import the updated JSON files into the live n8n instance (`knurdz3o.app.n8n.cloud`) and delete unused Supabase service-role credentials from scheduler workflows.

## Notes

- `wf1/wf3/wf4_*.json` are **snapshots exported from the live instance** after the 2026-07-12
  tenant-stamping (2.4) + autopilot-rewire (1.5) edits, then re-wired again 2026-07-17 (second
  pass, see above). They are NOT generated by `build_n8n_workflow_exports.js` — if you edit these
  workflows live again, re-export the snapshot (via the n8n MCP `get_workflow_details` or the n8n
  UI export) to keep the repo in sync.
- **Live activation status (2026-07-17, second pass — verified via n8n MCP):** `wf0a`, `wf0d`,
  `wf0e`, `wf0f`, `wf1`, `wf2`, `wf3`, `wf4`, `wf5`, `wf8b` are **active**. `wf8c` was
  **deactivated** this pass (superseded, see table above). `wf8d` remains **inactive** by design
  (gated on WF8b's platform credentials). `wf0d`/`wf0e` could not be verified/updated via MCP
  (workflow cards have "Available in MCP" turned off) — confirm their live node contents match
  these exports manually, or enable MCP access and re-run the sync. Real Gmail *sending* is still
  blocked on the `gmail.send` OAuth scope regardless of any workflow's active state (read-only
  today).
- **Every workflow above that calls `$vars.NEXUS_APP_URL` (WF2, WF3, WF4, WF8b, WF8d, the Approval
  Trigger) depends on `/api/internal/n8n/*` routes that exist on branch `issue-fix2` but NOT on
  `main`** (main has only 4 of ~20: `conversations`, `gmail-credentials`, `meta-credentials`,
  `workflow-logs`). Until `issue-fix2` is merged and deployed to production, these workflows will
  execute successfully in n8n but every call back into the app will 404. This is the top blocker —
  see `docs/NEXUS_REBUILD_CONTEXT.md` §2.
- HTTP nodes that call Supabase REST use the instance's **Supabase custom auth** credential —
  re-select it on import. Nodes that call the Next.js app (`Autopilot Send (policy-gated)`) use
  header-based `Authorization: Bearer {{ $vars.N8N_INGEST_TOKEN }}` — n8n Cloud blocks `$env` in
  node expressions, so this must be an n8n **Variable**, not an environment variable. Set
  `NEXUS_APP_URL` + `N8N_INGEST_TOKEN` as n8n Variables (Settings → Variables).
